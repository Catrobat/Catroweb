{% block subject %}
{% autoescape false %}
{{ 'passwordRecovery.subject'|trans({'%username%': user.username,
                                     '%confirmationUrl%': confirmationUrl,
                                     '%appplication_url%': url('index')
                                    }, 'catroweb') }}
{% endautoescape %}
{% endblock %}
{% block body_text %}
{% autoescape false %}
{{ 'passwordRecovery.message'|trans({'%username%': user.username,
                                     '%confirmationUrl%': confirmationUrl,
                                     '%appplication_url%': url('index'),
                                     '%loginPage%': url('login'),
                                     '%profilePage%': url('profile')
                                    }, 'catroweb') }}
{% endautoescape %}
{% endblock %}
{% block body_html %}{% endblock %}
