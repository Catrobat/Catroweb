{% extends '@SonataAdmin/CRUD/base_list_field.html.twig' %}

{% block field %}
  <div>
    <img src="{{ admin.getFeaturedImageUrl(object) }}" alt="Featured image" style="max-height: 80px; width: auto; border-radius: 6px; object-fit: cover;">
  </div>
{% endblock %}
