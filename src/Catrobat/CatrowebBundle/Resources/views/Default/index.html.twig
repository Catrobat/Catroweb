{% extends 'CatrowebBundle::base.html.twig' %}

{% block body %}

    {{ render(controller('CatrowebBundle:FeaturedProject:index', { _locale: app.request.locale, _embedded: true })) }}

    {{ render(controller('CatrowebBundle:Project:mostDownloaded', { _locale: app.request.locale, _embedded: true }, { 'limit': 18 })) }}

    {{ render(controller('CatrowebBundle:Project:mostViewed', { _locale: app.request.locale, _embedded: true }, { 'limit' : 18})) }}
	
{% endblock %}
