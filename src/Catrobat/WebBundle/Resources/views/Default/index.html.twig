{% extends 'CatrobatWebBundle::base.html.twig' %}

{% block body %}

    {{ render(controller('CatrobatWebBundle:FeaturedProgram:index', { _locale: app.request.locale, _embedded: true })) }}

    {{ render(controller('catrobat.web.program.controller:newestAction', { _locale: app.request.locale, _embedded: true }, { 'limit': 18 })) }}
    
    {{ render(controller('catrobat.web.program.controller:mostDownloadedAction', { _locale: app.request.locale, _embedded: true }, { 'limit': 18 })) }}

    {{ render(controller('catrobat.web.program.controller:mostViewedAction', { _locale: app.request.locale, _embedded: true }, { 'limit' : 18})) }}
	
{% endblock %}
