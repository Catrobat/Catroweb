{% extends 'CatrobatWebBundle::base.html.twig' %}

{% block body -%}
    <h1>FeaturedProject creation</h1>

    <form action="{{ path('_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('projects_featured') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
