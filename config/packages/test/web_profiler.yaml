web_profiler:
    toolbar: false
    intercept_redirects: false

framework:
    test: ~
    session:
        storage_id: session.storage.mock_file
    profiler:
        enabled: true
        collect: true
